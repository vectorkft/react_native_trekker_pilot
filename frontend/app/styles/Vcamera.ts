import {StyleSheet} from 'react-native';

export const vcamera = StyleSheet.create({
  buttonPosition: {
    position: 'absolute',
    top: 0,
    left: 0,
  },
  buttonStyle: {
    backgroundColor: 'transparent',
  },
});
